[package]
name = "booth-visualization"
version = "0.1.0"
edition = "2024"

[dependencies]
yew = { version = "0.22", features = ["csr"] }
wasm-bindgen = "0.2"
web-sys = "0.3"

[profile.release]
opt-level = 3
lto = true
